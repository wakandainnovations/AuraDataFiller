db.url=jdbc:postgresql://localhost:5432/aura
db.user=
db.password=

# Anthropic API key — used by Claude Haiku for event detection
# Get one at https://console.anthropic.com/
anthropic.api.key=

# YouTube Data API v3 key — used by the YouTube promo-metrics enrichment service
# (--youtube-scan). Create one at https://console.cloud.google.com/apis/credentials
# after enabling "YouTube Data API v3" for a project.
youtube.api.key=
